/* HEADER */

let menus = document.querySelectorAll('.top_nav > .containar > nav > ul > li');
let topNav = document.querySelector('.top_nav')


menus.forEach(item => {
  item.addEventListener('mouseenter', () => {
    let subMenuHeight = querySelector('ul').offsetHeight;
  })
})